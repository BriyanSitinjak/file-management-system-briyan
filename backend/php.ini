; Used by ./serve.sh / composer serve.
; Also installed system-wide at /opt/homebrew/etc/php/8.5/conf.d/99-uploads.ini
; so plain `php artisan serve` picks up the same limits.

file_uploads = On
upload_max_filesize = 64M
post_max_size = 64M
max_file_uploads = 20
